import { Box, styled } from "@mui/material";

export const SpecialistsWrapper = styled(Box)`
  .swiper-pagination {
    position: relative;
    gap: 12px;
    display: flex;
    justify-content: center;
    gap: 4px;
    margin-top: 36px;
    padding-bottom: 2px;
  }
  .swiper-pagination-bullet {
    height: 10px;
    width: 10px;
  }
`;
